machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install -U docker-compose
    - docker-compose --version

test:
  override:
    - sudo make test
    
deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
